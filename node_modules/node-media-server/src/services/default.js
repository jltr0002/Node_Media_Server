/**
 * Handles the stream with default behavior.
 * For example, simply logs the stream or forwards it without conversion.
 *
 * @param {object} streamInfo - Information about the stream (e.g., name, URL, etc.).
 */
function handleDefaultStream(streamInfo) {
    console.log(`Handling default stream: ${streamInfo.name}`);
    // Add your default logic here, for example:
    // - Forward the stream to another process
    // - Save or re-route the stream without modification
  }
  
  module.exports = {
    handleDefaultStream,
  };
  
  